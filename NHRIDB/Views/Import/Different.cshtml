@*@model  NHRIDB.Models.ViewModels.DiffViewModel*@
@model  NHRIDB.Models.ViewModels.ImportAnsViewModel

@{
    ViewBag.Title = "匯入資料的總數差異表";
}

@*<div class="msg">
    測試-2021/7/12->結果已寫入DB,剩回傳結果確認!!
</div>*@

<div class="msg">
    @Model.StrisSuccess
    <br />
    @Model.StrMesage
    <br />
    前一版檔案個案數:@Model.OldCount
    <br />
    新上傳檔案個案數:@Model.NewCount
</div>


@*@{
        if (Model.isSuccess)
        {
        <div class="msg">
            @Model.Test 匯入完成
        </div>
        }
        else
        {
    <div class="msg">
        @Model.Test 匯入失敗,@Model.StrMsg
    </div>
            <div>
                StackTrace:@Model.StackTrace
            </div>
        }
    }

    <table class="table table-hover table-bordered">
        <thead>
            <tr>
                <th>

                </th>
                @foreach (var column in Model.columns)
                {
                    <th>@column.DisplayName</th>
                }

            </tr>
        </thead>

        @for (int i = 0; i < Model.datas.Count; i++)
        {
            var item = Model.datas[i];
            <tr>
                <td>
                    @item.type
                </td>
                @foreach (var column in Model.columns)
                {

                    var value = item.GetType().GetProperty(column.Name).GetValue(item);


                    <td>
                        @value

                    </td>



                }
            </tr>
        }
    </table>*@

<a class="btn" href="@Url.Action("Index")">離開</a>